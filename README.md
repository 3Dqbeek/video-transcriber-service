# Video Transcriber Service

Современный веб-сервис для транскрибации видео с определением ролей говорящих на основе T-one framework.

**Автор:** Vasiliy Dautov  
**Лицензия:** MIT License

## ✨ Возможности

- **📺 Транскрибация из URL** - поддержка Rutube, YouTube и других платформ
- **📁 Транскрибация локальных файлов** - загрузка видео файлов через веб-интерфейс
- **🎭 Определение ролей** - автоматическое определение Operator/Customer/Unknown
- **📄 Множественные форматы** - экспорт в TXT и JSON
- **🌐 Современный веб-интерфейс** - красивый и удобный UI
- **📊 Отслеживание задач** - мониторинг прогресса в реальном времени
- **⚡ Высокая производительность** - обработка длинных видео (15x реального времени)

## 📋 Системные требования

### Обязательные зависимости:
- **Python 3.8+**
- **T-one framework** (из GitHub)
- **FFmpeg** (для обработки видео)
- **Git** (для клонирования репозитория)

### Установка системных зависимостей:

#### Ubuntu/Debian:
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv git ffmpeg
```

#### CentOS/RHEL:
```bash
sudo yum install python3 python3-pip git
sudo yum install epel-release
sudo yum install ffmpeg
```

#### macOS:
```bash
brew install python3 git ffmpeg
```

## 🚀 Быстрый старт

### 1. Установка T-one framework

**Важно!** Сначала необходимо установить T-one framework из GitHub:

```bash
# Клонируйте репозиторий T-one
git clone https://github.com/voicekit-team/T-one.git
cd T-one

# Создайте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# Установите T-one framework
pip install -e .
```

### 2. Установка Video Transcriber Service

```bash
# Клонируйте репозиторий Video Transcriber Service
git clone https://github.com/your-username/video-transcriber-service.git
cd video-transcriber-service

# Активируйте виртуальное окружение T-one
source ../T-one/.venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Запуск сервиса

```bash
python3 run_service.py
```

### 4. Открытие веб-интерфейса

Откройте браузер и перейдите по адресу: **http://localhost:8086**

## 📖 Использование

### Веб-интерфейс

1. **Транскрибация из URL:**
   - Вставьте ссылку на видео (Rutube, YouTube и др.)
   - Выберите формат вывода (TXT или JSON)
   - Нажмите "Начать транскрибацию"

2. **Транскрибация файла:**
   - Перетащите видео файл в область загрузки
   - Или нажмите для выбора файла
   - Выберите формат вывода
   - Нажмите "Начать транскрибацию"

3. **Мониторинг задач:**
   - Перейдите на вкладку "Задачи"
   - Просматривайте статус всех задач
   - Скачивайте готовые результаты

### API Endpoints

- `POST /api/transcribe-url` - транскрибация по URL
- `POST /api/transcribe-file` - транскрибация файла
- `GET /api/status/{task_id}` - статус задачи
- `GET /api/download/{task_id}` - скачивание результата
- `GET /api/tasks` - список всех задач

## 📁 Структура проекта

```
video-transcriber-service/
├── app.py                          # Основное веб-приложение
├── streaming_video_transcriber.py  # Транскрибатор
├── run_service.py                 # Скрипт запуска
├── check_installation.py          # Скрипт проверки установки
├── requirements.txt               # Зависимости
├── README.md                      # Документация
├── LICENSE                        # Лицензия
├── .gitignore                     # Git ignore файл
├── transcriptions/                # Результаты транскрибации
├── static/                        # Статические файлы
└── templates/                     # Шаблоны
```

## 🔧 Конфигурация

### Переменные окружения

- `PORT` - порт сервера (по умолчанию: 8086)
- `HOST` - хост сервера (по умолчанию: 0.0.0.0)

### Настройки транскрибатора

В файле `streaming_video_transcriber.py` можно настроить:

- `output_dir` - директория для сохранения результатов
- `use_gpu` - использование GPU ускорения
- Форматы вывода и параметры обработки

## 📊 Поддерживаемые форматы

### Входные форматы видео
- **URL:** Rutube, YouTube, Vimeo и другие
- **Файлы:** MP4, AVI, MOV, MKV, WEBM

### Выходные форматы
- **TXT:** Текстовый файл с временными метками
- **JSON:** Структурированные данные с метаинформацией

## 🎯 Примеры использования

### Транскрибация Rutube видео

```python
from streaming_video_transcriber import StreamingVideoTranscriber

transcriber = StreamingVideoTranscriber()
transcriber.init_pipeline()

# Транскрибация по URL
transcript, output_file = transcriber.transcribe_video(
    "https://rutube.ru/video/...",
    "txt"
)
```

### Транскрибация локального файла

```python
# Транскрибация локального файла
transcript, output_file = transcriber.transcribe_video(
    "/path/to/video.mp4",
    "json"
)
```

## 🛠️ Технические детали

- **Основа:** T-one framework для ASR
- **Веб-фреймворк:** FastAPI
- **Обработка видео:** yt-dlp, ffmpeg
- **Обработка аудио:** librosa, soundfile
- **Определение ролей:** Keyword-based detection

## 📈 Производительность

- **Скорость обработки:** 15x реального времени
- **Поддержка длинных видео:** до нескольких часов
- **Память:** эффективное использование с потоковой обработкой
- **Стабильность:** обработка ошибок и восстановление

## 🔍 Мониторинг

Сервис предоставляет:

- Статистику задач в реальном времени
- Логи обработки
- Прогресс-бары для длительных операций
- Автоматическое обновление статуса

## 🚨 Устранение неполадок

### Частые проблемы

1. **Ошибка "T-one framework не найден":**
   ```bash
   # Убедитесь, что T-one установлен
   cd T-one
   source .venv/bin/activate
   pip install -e .
   
   # Проверьте установку
   python -c "import tone; print('T-one установлен')"
   ```

2. **Ошибка "python command not found":**
   ```bash
   # Используйте python3 вместо python
   python3 run_service.py
   
   # Или создайте симлинк
   sudo ln -s /usr/bin/python3 /usr/bin/python
   ```

3. **Ошибка "ffmpeg not found":**
   ```bash
   # Ubuntu/Debian
   sudo apt install ffmpeg
   
   # CentOS/RHEL
   sudo yum install ffmpeg
   
   # macOS
   brew install ffmpeg
   ```

4. **Ошибка инициализации пайплайна:**
   - Проверьте установку T-one framework
   - Убедитесь в наличии модели
   - Проверьте виртуальное окружение

5. **Ошибка скачивания видео:**
   - Проверьте доступность URL
   - Убедитесь в наличии интернет-соединения
   - Проверьте, что видео не приватное

6. **Ошибка обработки файла:**
   - Проверьте формат видео файла
   - Убедитесь в наличии ffmpeg
   - Проверьте права доступа к файлу

7. **Порт 8086 занят:**
   ```bash
   # Найдите процесс на порту
   sudo lsof -i :8086
   
   # Остановите процесс
   sudo kill -9 <PID>
   
   # Или используйте другой порт
   uvicorn app:app --port 8087
   ```

### Проверка установки

#### Автоматическая проверка:
```bash
cd video-transcriber-service
source ../T-one/.venv/bin/activate
python3 check_installation.py
```

#### Ручная проверка:
```bash
# Проверьте все компоненты
python3 --version  # Должно быть 3.8+
ffmpeg -version     # Должен быть установлен
git --version       # Должен быть установлен

# Проверьте T-one
cd T-one
source .venv/bin/activate
python -c "import tone; print('✅ T-one OK')"

# Проверьте зависимости сервиса
cd video-transcriber-service
source ../T-one/.venv/bin/activate
python -c "import fastapi, uvicorn; print('✅ Зависимости OK')"
```

### Логи

Логи сервиса выводятся в консоль и содержат подробную информацию о процессе обработки:

```bash
# Запуск с подробными логами
python3 run_service.py

# Логи в файл
python3 run_service.py > service.log 2>&1 &
```

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 Лицензия

Проект использует T-one framework и следует его лицензионным условиям.

## 🙏 Благодарности

- [T-one framework](https://github.com/voicekit-team/T-one) - основа для ASR
- [FastAPI](https://fastapi.tiangolo.com/) - веб-фреймворк
- [yt-dlp](https://github.com/yt-dlp/yt-dlp) - скачивание видео

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи сервиса
2. Убедитесь в корректности входных данных
3. Проверьте доступность всех зависимостей
4. Создайте Issue в репозитории

---

**Video Transcriber Service** - мощный инструмент для транскрибации видео с современным веб-интерфейсом! 🎉
